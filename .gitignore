# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
.env

# --- OS ---
Thumbs.db
.DS_Store
desktop.ini

# --- Large GIS rasters (documented in CdL/input_data/LARGE_DATA_MANIFEST.md, do NOT commit) ---
dem_cdl.tif
COSc_*.tif
*.tif.aux.xml

# --- Model / PEST working outputs (these live outside the repo; ignore if run in-tree by mistake) ---
CdL_model/
CdL_pest/
_output/
_input/
master/
workers/
org/
template/
tri/
*.hds
*.cbc
*.lst
mfsim.lst

# NOTE: the small bundled inputs under CdL/input_data/ ARE committed on purpose
# (gpkg, forcing csv, soils tif, grid caches, npz). Only the two large rasters above are excluded.

# Spyder project
.spyproject/
